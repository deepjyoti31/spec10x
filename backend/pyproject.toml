[project]
name = "spec10x-backend"
version = "0.1.0"
description = "Spec10x Backend API — Interview Intelligence Platform"
requires-python = ">=3.12"
license = { text = "MIT" }

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
