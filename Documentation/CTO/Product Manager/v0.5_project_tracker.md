# Spec10x v0.5x Release Tracker

> Date: March 16, 2026
> Status: Sprint 0 package drafted and ready for G0 review
> Owner: Founder acting as Product Manager and Product Owner
> Team: Solo founder plus AI coding agents
> Parent plan: `Documentation/CTO/v0.5_planning.md`
> Architecture reference: `Documentation/CTO/technical_architecture.md`

---

## 1. Purpose

This document is the operational tracker for the `v0.5x` release family:

- `v0.5`
- `v0.51`
- `v0.52`
- optional `v0.53`

It now does four jobs, not one:

1. controls scope and cut lines
2. controls release gates and measurement windows
3. tracks the current release backlog at story level
4. holds later releases as forecast backlog only

Important rule:

- only `v0.5` is detailed enough to become sprint-committed today
- `v0.51+` remain forecast backlog until the prior release has shipped and been measured

That rule fixes the earlier contradiction between "do not start the next release until the current one is measured" and the old back-to-back calendar.

---

## 2. Operating Model

### 2.1 Delivery assumptions

- Sprint length: `1 week`
- Nominal capacity: `20 points`
- Hard committed capacity: `18 points`
- Reserve buffer: `2 points`
- Estimation scale: `1, 2, 3, 5, 8`
- Any story larger than `8` must be split before commitment

### 2.2 Planning levels and workflow states

Use these states consistently:

- `Outline` - forecast placeholder, not commitment-ready
- `Draft` - shaped enough to discuss, but still missing readiness inputs
- `Ready` - meets Definition of Ready and can enter the next sprint
- `In Sprint`
- `In Review`
- `Accepted`
- `Released`
- `Measured`

Rule:

- only `Ready` stories may enter a sprint
- all `v0.51+` stories stay in `Outline` until the previous release passes its measurement gate

### 2.3 Definition of Ready

A story is `Ready` only if all of the following are true:

- the user problem is clear
- the story maps to exactly one epic and one PRD
- acceptance criteria are testable
- dependencies and assumptions are listed
- the story fits inside one sprint
- acceptance evidence is pre-declared
- copy, trust, or observability needs are called out if the work is operationally sensitive

### 2.4 Definition of Done

A story is `Done` only if all of the following are true:

- implementation is complete
- acceptance criteria pass
- tests were added or updated where appropriate
- docs or UI copy were updated if behavior changed
- metrics or logs exist for operationally sensitive work
- required acceptance evidence is attached
- the founder acting as Product Owner has accepted the result

### 2.5 Required acceptance evidence

Every accepted story should link or point to all applicable evidence:

- PR or commit reference
- automated tests by name or test file
- screenshot, short demo clip, or before/after note for UI-visible work
- metrics or log sample for operational work
- documentation or trust-copy update note

### 2.6 Scope control rules

Stories are classified as:

- `Must` - required for the release ship decision
- `Should` - valuable, but cut before threatening release quality
- `Could` - only pull in if the release is green late in the cycle

Rules:

- only `Must` stories fill committed sprint points
- `Should` and `Could` items may be pulled only if the sprint is green by Thursday review
- no new `Must` stories after Sprint 2 without an explicit scope trade
- no new `Should` or `Could` stories after Sprint 5
- release freeze starts 48 hours before a ship gate; only defect fixes, copy fixes, and instrumentation fixes may merge

---

## 3. Release Control Board

This is the top-level control view for the whole release family.

| Item | Build Window | Measure Window | Status | Gate Status | Current Cut Line | Notes |
|---|---|---|---|---|---|---|
| Sprint 0 readiness | Mar 16 - Mar 22, 2026 | n/a | Green | Ready for `G0` review | n/a | All six decisions are closed; five `v0.5` PRDs and trust promises are drafted; first two sprints are loaded to <= 18 committed points |
| `v0.5` | Mar 23 - May 10, 2026 | May 11 - May 24, 2026 | Yellow | Not started | `Must` only | Zendesk selected as first support connector; `US-05-04-05` and `US-05-05-05` remain below the line |
| `v0.51` | May 25 - Jun 21, 2026 | Jun 22 - Jul 05, 2026 | Blocked | Waiting on `v0.5` measurement gate | Forecast only | Starts only if `v0.5` ships cleanly and is measured |
| `v0.52` | Jul 06 - Aug 02, 2026 | Aug 03 - Aug 16, 2026 | Blocked | Waiting on `v0.51` measurement gate | Forecast only | Later dates moved to preserve measurement discipline |
| optional `v0.53` | Aug 17 - Sep 06, 2026 | Sep 07 - Sep 20, 2026 | Blocked | Waiting on `v0.52` measurement gate | Forecast only | Do only if demand and support burden justify it |

Status legend:

- `Green` - on track, no material gate risk
- `Yellow` - real risk exists, but still controllable
- `Red` - release plan or ship date is no longer credible without intervention
- `Blocked` - intentionally held behind a required gate

---

## 4. Sprint 0 Decision Register

Sprint 0 exists to close the decisions that would otherwise create fake certainty.

| Decision ID | Decision | Due | Owner | Current State | Decision Rule | Blocked Work |
|---|---|---|---|---|---|---|
| `D-05-01` | Choose the first support connector target | Mar 18 | Founder | Decided - Zendesk | Zendesk selected as the first support connector target for `v0.5` | `EPIC-05-02` |
| `D-05-02` | Approve `signals` schema v1 | Mar 18 | Founder | Decided - approved additive normalized evidence layer | `signals` v1 is approved as the shared evidence layer for feed, score, and source-aware theme views without replacing the current interview-native data model | `EPIC-05-01`, `EPIC-05-03`, `EPIC-05-04`, `EPIC-05-05` |
| `D-05-03` | Decide ownership model for new source tables | Mar 19 | Founder | Decided - workspace-safe ownership model | New source tables are workspace-owned from day one, with a default personal workspace pattern preserving current single-user behavior | `EPIC-05-01`, `EPIC-051-01`, `EPIC-053-03` |
| `D-05-04` | Choose frontend smoke test runner and CI path | Mar 19 | Founder | Decided - Playwright smoke suite via Cloud Build | Use Playwright with Chromium-only smoke coverage as the ship-blocking frontend suite for `v0.5` | `US-05-06-01` |
| `D-05-05` | Define sync observability baseline | Mar 20 | Founder | Decided - `sync_runs` plus structured logs baseline | Use `sync_runs`, structured logs, and a founder review dashboard as the minimum observability layer for `v0.5` | `US-05-06-04`, release gates |
| `D-05-06` | Approve minimum trust package contents | Mar 20 | Founder | Decided - trust package and customer promises approved | Publish clear promises on permissions, storage, delete/disconnect behavior, security controls, and model-data handling | `US-05-06-03`, `v0.51` trust work |

Sprint 0 exit rule:

- Sprint 1 does not start until all six decision rows are either `Decided` or explicitly deferred with a written consequence and scope adjustment

### 4.1 `signals` schema v1 decision

Decision summary:

- approve `signals` as an additive normalized evidence layer for `v0.5`
- do not replace `interviews`, `insights`, `themes`, or `transcript_chunks` in `v0.5`
- use `signals` as the shared query layer for unified feed, source filters, score inputs, and source-aware theme evidence
- use `workspace_id` as the primary ownership field for new source tables, while current user-scoped APIs resolve through a default personal workspace mapping

Why this is the right cut for `v0.5`:

- the current codebase already depends on `interviews`, `insights`, `themes`, and `transcript_chunks` for upload, synthesis, and Ask
- replacing that model now would create unnecessary rewrite risk
- adding `signals` lets multi-source work land without breaking the proven v0.1 interview path

Approved supporting table minimums:

| Table | Purpose | Minimum columns |
|---|---|---|
| `data_sources` | catalog of supported source types and providers | `id`, `source_type`, `provider`, `display_name`, `connection_method`, `is_active`, timestamps |
| `source_connections` | one workspace's connection or import pathway for one provider | `id`, `workspace_id`, `created_by_user_id`, `data_source_id`, `status`, `secret_ref`, `config_json`, `last_synced_at`, `last_error_summary`, timestamps |
| `sync_runs` | audit trail of backfills, incrementals, and manual re-syncs | `id`, `source_connection_id`, `run_type`, `status`, `started_at`, `finished_at`, `cursor_in`, `cursor_out`, `records_seen`, `records_created`, `records_updated`, `error_summary` |
| `source_items` | idempotent mapping from upstream records to internal records | `id`, `workspace_id`, `source_connection_id`, `external_id`, `source_record_type`, `external_updated_at`, `native_entity_type`, `native_entity_id`, `checksum`, `first_seen_at`, `last_seen_at`; unique on `source_connection_id + external_id` |
| `signals` | normalized evidence rows consumed by feed, score, and source-aware evidence views | `id`, `workspace_id`, `source_connection_id`, `source_item_id`, `source_type`, `provider`, `signal_kind`, `occurred_at`, `title`, `content_text`, `author_or_speaker`, `sentiment`, `source_url`, `metadata_json`, `native_entity_type`, `native_entity_id`, `status`, timestamps |

Approved `signals` v1 field rules:

- `source_type` is one of: `interview`, `support`, `survey`, `analytics`
- `provider` is provider-specific but not product-logic-specific, for example `native_upload`, `zendesk`, `csv_import`, `fireflies`, `mixpanel`
- `signal_kind` describes the normalized evidence unit, not the provider payload, for example `insight`, `ticket`, `survey_response`, `metric_window`
- `content_text` is the main evidence text used by feed rendering, scoring inputs, and later theme evidence views
- `metadata_json` holds provider-specific richness that should not leak into core product logic
- `native_entity_type` and `native_entity_id` link signals back to the current native data model so the UI can deep-link without rewriting v0.1 internals
- `status` should support at least `active`, `hidden`, and `error`

Approved source mapping rules:

- interview uploads map to one signal per accepted `insight`, with `provider = native_upload`, `native_entity_type = insight`, and `native_entity_id = insights.id`
- Zendesk maps to one signal per normalized ticket evidence item defined in `PRD-05-02`; the exact ticket-versus-comment rule belongs in the PRD, not in the core schema
- survey and NPS CSV imports map to one signal per response row, with `provider = csv_import`
- future analytics providers map to one signal per normalized event aggregate or metric window, not to raw event payload spam

Non-goals for `signals` v1:

- do not store raw secrets or auth tokens anywhere in source tables
- do not store giant full provider payloads in `signals`
- do not force Ask or transcript search onto `signals`; Ask remains grounded in `transcript_chunks`
- do not add Zendesk-only fields to core `signals` columns
- do not rewrite the existing interview-theme-insight system during `v0.5`

Decision consequence:

- `EPIC-05-01`, `EPIC-05-03`, `EPIC-05-04`, and `EPIC-05-05` may now design against this approved schema contract
- workspace-safe ownership details are closed by `D-05-03`

### 4.2 Ownership model decision

Decision summary:

- `data_sources` remains global and not customer-owned
- `source_connections`, `source_items`, and `signals` are workspace-owned from day one
- `sync_runs` inherit ownership through `source_connection_id`
- `source_connections` must store `created_by_user_id` for audit and support context
- `v0.5` can use a default personal workspace pattern behind the scenes, but no multi-user workspace UI ships in `v0.5`
- current user-scoped app surfaces should resolve the caller to their default workspace without changing the visible product model

Boundary rules:

- do not retrofit the existing `interviews`, `insights`, `themes`, or `transcript_chunks` tables to full workspace ownership during `v0.5`
- do not block source foundation work on invites, roles, collaboration, or workspace admin UX
- do ensure every new source table and index can survive a later move to true shared workspaces without another ownership rewrite

Decision consequence:

- `PRD-05-01` must include the default personal workspace bootstrap assumption
- new connector work should never assume a source connection is permanently personal-user-owned

### 4.3 Frontend smoke coverage decision

Decision summary:

- use Playwright as the `v0.5` frontend smoke test runner
- the `v0.5` ship blocker is smoke coverage, not a broader unit-test framework decision
- run Chromium-only in CI for `v0.5` to keep setup small and reliable

Approved suite shape:

- test location: `frontend/e2e/smoke/`
- target command shape: `npm run test:smoke`
- execution path: build app, start preview server, run Playwright smoke suite
- CI path: add a Cloud Build step after frontend build and before deploy

Minimum smoke coverage:

- integrations shell renders and loads provider state
- survey import happy path reaches preview and completion UI
- unified feed renders mixed-source rows
- source filters apply without breaking page state
- priority board renders ranked themes
- critical nav path between dashboard, feed or theme detail, and board is intact

Decision consequence:

- `US-05-06-01` should implement this exact baseline, not an open-ended frontend testing strategy

### 4.4 Sync observability baseline decision

Decision summary:

- `sync_runs` is the durable audit source of truth
- structured JSON logs go to Cloud Logging for operational debugging
- exceptional failures continue to surface through Sentry where appropriate
- founder review happens through one saved dashboard or query view, not through a new in-product admin UI in `v0.5`

Required metrics:

- sync success rate by provider
- sync failure rate by provider
- average backfill duration
- duplicate import rate
- time from connection or import start to first useful insight
- last successful sync per active connection
- records seen, created, and updated per sync run

Initial alert thresholds:

- 3 consecutive failed syncs on the same active connection
- no successful sync in 24 hours for an active production connection
- duplicate import rate above 2 percent on a sync run
- backfill duration above 30 minutes for a pilot connection
- time-to-first-insight above 60 minutes after connection kickoff

Decision consequence:

- `US-05-06-04` should implement these metrics and thresholds as the minimum pilot review baseline

### 4.5 Trust package decision

Decision summary:

- `v0.5` ships with a customer-facing trust package, not just internal notes
- trust promises must stay plain-English and must match actual product behavior
- do not promise zero retention, provider-side deletion, or model-provider behavior that has not been verified in production settings

Approved trust package assets:

- one-page security and trust overview
- plain-English permissions summary by connector type
- storage and retention summary
- disconnect and imported-data delete guide
- subprocessor list
- privacy and terms language review checklist
- customer-facing trust promises document

Approved trust promises:

- Spec10x asks for read-only access where possible
- Spec10x stores the minimum durable copy needed to make the product useful
- Spec10x stores provider credentials in Secret Manager references, not ordinary app tables
- disconnect stops future syncs in Spec10x
- imported-data delete removes Spec10x's copied data, not upstream provider records
- Spec10x does not use customer content to train Spec10x's own generalized models
- Spec10x will not claim stronger model-provider retention guarantees than the production configuration can support

Decision consequence:

- `US-05-06-03` should ship against this trust package scope and promise set

---

## 5. Active Risk Register

This is the live release risk register, not a one-time appendix.

| Risk | Likelihood | Impact | Trigger | Mitigation | Contingency | Owner |
|---|---|---|---|---|---|---|
| Zendesk proves to be the wrong first support provider | Medium | High | Pilot demand shifts hard away from Zendesk or implementation friction is materially worse than expected | Reconfirm pilot demand and API practicality before Sprint 2 commitment | If confidence drops, keep source foundation and survey scope, and move the live support connector behind a revised gate | Founder |
| Weak `signals` model causes special-case code | Medium | High | Feed, score, or theme logic starts branching by provider | Freeze schema in Sprint 0 and validate against support, survey, and analytics examples | Hold new connector work until schema is corrected | Founder |
| Ownership model mismatch for company data | Medium | High | Connected sources behave like personal assets | Ship new source tables as workspace-owned from day one and keep the visible UX on a default personal workspace pattern in `v0.5` | Keep visible `v0.5` behavior single-user and document ownership boundaries clearly in copy and docs | Founder |
| Frontend smoke coverage lands too late | High | Medium | UI regressions are found manually near ship week | Choose test runner in Sprint 0 and get CI stub in place by Sprint 2 | Cut `Should` and `Could` scope before ship if smoke coverage is weak | Founder |
| Sync behavior is flaky or non-idempotent | Medium | High | Duplicate imports, cursor drift, or retry bugs appear | Contract tests, replay fixtures, explicit cursor logic, sync logs | Hold release and fix connector behavior before broad pilot rollout | Founder |
| Trust copy does not match actual behavior | Medium | High | Review finds copy promises the product does not keep | Draft trust package early and review against implemented behavior | Block ship gate until copy and behavior match | Founder |
| Score v1 feels magical or untrustworthy | Medium | High | Pilots cannot explain rank order or disagree strongly with it | Keep score simple, show breakdown, measure trust explicitly | Hold later scoring work and improve evidence presentation first | Founder |
| Optional scope crowds out core wedge | High | Medium | Saved views or collections start delaying core backlog | Keep `Should` and `Could` below line until all `Must` stories are green | Cut optional work immediately when a `Must` sprint goes yellow | Founder |
| Backfills create unexpected cost or rate-limit issues | Medium | Medium | Long histories make sync slow or expensive | Bound historical window, track sync duration and API failure rate | Reduce backfill window for pilot accounts and document limits | Founder |

Risk review cadence:

- Monday planning: refresh status and owners
- Thursday forecast review: decide whether any `Should` or `Could` work should be cut
- Friday sprint review: update mitigations and contingency calls

---

## 6. PRD Stack And Readiness

The release should be run through smaller PRDs, not one umbrella document.

| PRD ID | Release | Purpose | Current State | Needed By | Minimum Ready Condition | Artifact |
|---|---|---|---|---|---|---|
| `PRD-05-01` | `v0.5` | Source foundation and integrations shell | Drafted | Mar 18 | data model, ownership model note, sync lifecycle, non-goals | `Documentation/CTO/Product Manager/PRD-05-01_source_foundation_and_integrations_shell.md` |
| `PRD-05-02` | `v0.5` | Zendesk support connector MVP | Drafted | Mar 19 | Zendesk auth path, sync path, retry and delete behavior | `Documentation/CTO/Product Manager/PRD-05-02_zendesk_support_connector_mvp.md` |
| `PRD-05-03` | `v0.5` | Survey and NPS import MVP | Drafted | Mar 19 | CSV contract, validation rules, preview behavior, failure handling | `Documentation/CTO/Product Manager/PRD-05-03_survey_and_nps_import_mvp.md` |
| `PRD-05-04` | `v0.5` | Unified feed and source-aware themes | Drafted | Mar 20 | feed row model, filters, evidence linkback, trust wording | `Documentation/CTO/Product Manager/PRD-05-04_unified_feed_and_source_aware_themes.md` |
| `PRD-05-05` | `v0.5` | Impact score v1 and priority board | Drafted | Mar 20 | score inputs, explainability pattern, board scope, non-goals | `Documentation/CTO/Product Manager/PRD-05-05_impact_score_v1_and_priority_board.md` |
| `PRD-051-01` | `v0.51` | Fireflies automatic interview sync | Outline | Open after `v0.5` measurement gate | forecast only; do not push to `Draft` early | not drafted yet |
| `PRD-052-01` | `v0.52` | First analytics connector | Outline | Open after `v0.51` measurement gate | forecast only; do not push to `Draft` early | not drafted yet |
| `PRD-052-02` | `v0.52` | Score v2 and trends | Outline | Open after `v0.51` measurement gate | forecast only; do not push to `Draft` early | not drafted yet |
| `PRD-053-01` | `v0.53` | Second interview connector and connector hardening | Outline | Open after `v0.52` measurement gate | forecast only; do not push to `Draft` early | not drafted yet |

Rule:

- no story can move to `Ready` until its parent PRD is at least `Draft`

### 6.1 Supporting Sprint 0 artifacts

| Artifact | Release | Current State | File |
|---|---|---|---|
| Trust promises | `v0.5` | Drafted | `Documentation/CTO/Product Manager/v0.5_trust_promises.md` |
| Sprint 0 decisions | `v0.5` | Closed in tracker | Section 4 of this tracker |

---

## 7. Release Scope And Cut Lines

### 7.1 Release `v0.5`

Release goal:

> Prove that Spec10x can combine interviews with other customer evidence and help a PM see what matters in one place.

Release exit criteria:

- source model exists and is stable enough for future connectors
- one live Zendesk support source is working
- survey or NPS CSV import works end to end
- mixed-source feed is usable
- themes show source-aware evidence
- score v1 is inspectable
- priority board is usable in pilot environments
- sync status, logs, and release metrics are reviewable

Severity threshold for ship:

- `0` open Sev-1 defects
- at most `2` open Sev-2 defects with founder-approved workaround and written follow-up

Scope cut lines:

| Class | Included Work |
|---|---|
| `Must` | `EPIC-05-01`, `EPIC-05-02`, `EPIC-05-03`, `EPIC-05-04` without `US-05-04-05`, `EPIC-05-05` without `US-05-05-05`, `EPIC-05-06` |
| `Should` | `US-05-04-05` saved views |
| `Could` | `US-05-05-05` collections and theme merge |

### 7.2 Release `v0.51`

Release goal:

> Remove manual interview upload as the primary workflow for supported users.

Important note:

- `v0.51` is forecast only until the `v0.5` measurement gate passes

### 7.3 Release `v0.52`

Release goal:

> Add the first analytics source and improve score trust without over-claiming correlation.

Important note:

- `v0.52` is forecast only until the `v0.51` measurement gate passes

### 7.4 Optional Release `v0.53`

Release goal:

> Prove connector reuse and improve operational confidence.

Important note:

- `v0.53` is optional and should be opened only if demand and support burden justify it

---

## 8. Release Gate Calendar

These gates make the release family governable.

| Gate ID | Window | Decision | Required Evidence | Allowed Outcomes |
|---|---|---|---|---|
| `G0` | Mar 21 - Mar 22, 2026 | Sprint 0 readiness gate | six decisions closed, five `v0.5` PRDs drafted, trust promises drafted, first two sprints loaded to <= 18 committed points, risks reviewed | Go to Sprint 1 / Hold and re-plan |
| `G1` | May 08 - May 10, 2026 | `v0.5` ship gate | release exit criteria met, smoke tests pass, failure-path tests pass, trust docs match behavior, observability is live, rollback plan exists | Ship / Hold / Cut `Should` and `Could` then re-evaluate |
| `G1M` | May 22 - May 24, 2026 | `v0.5` measurement gate | two weeks of metric review, pilot feedback summary, score trust review, board usage review | Advance to `v0.51` / Extend measurement / Rework `v0.5` first |
| `G2` | Jun 19 - Jun 21, 2026 | `v0.51` ship gate | connection flow, backfill, incremental sync, disconnect, delete, trust docs, auditability all pass | Ship / Hold |
| `G2M` | Jul 03 - Jul 05, 2026 | `v0.51` measurement gate | connection success, backfill duration, sync success, trust friction review | Advance to `v0.52` / Extend measurement / Improve onboarding and trust first |
| `G3` | Jul 31 - Aug 02, 2026 | `v0.52` ship gate | analytics connector, trend UX, score explainability, correlation guardrails, performance checks pass | Ship / Hold |
| `G3M` | Aug 14 - Aug 16, 2026 | `v0.52` measurement gate | analytics clarity review, trend comprehension review, noise and false-correlation review | Open `v0.53` / Stay with `v0.52` hardening / Simplify before expanding |

---

## 9. `v0.5` Committed Backlog

Current planning status:

- Sprint 0 decisions are closed
- all five `v0.5` PRDs are drafted
- `v0.5` stories remain `Draft` until acceptance evidence fields are populated and the sprint commitment is confirmed

### `EPIC-05-01` Source Foundation And Integrations Shell

PRD:

- `PRD-05-01`

Epic goal:

- create the reusable source, connection, sync, and status foundation needed for all later connectors

Dependencies:

- `D-05-01` first support provider decision
- `D-05-02` `signals` schema approval
- `D-05-03` ownership model decision

Exit criteria:

- source model exists
- sync lifecycle exists
- integrations shell is visible in product

| Story ID | Pts | State | Class | Target Sprint | Ready Requirements | Acceptance Evidence |
|---|---:|---|---|---|---|---|
| `US-05-01-01` | 3 | Draft | Must | Sprint 1 | catalog table exists; provider and source type keys are unique; connections can reference source records | migration, ORM test, seed or query output |
| `US-05-01-02` | 3 | Draft | Must | Sprint 1 | connection record tracks provider, status, workspace scope, creator audit fields, and last sync; status transitions are defined; list and detail queries work | migration, API test, sample response |
| `US-05-01-03` | 5 | Draft | Must | Sprint 1 | `sync_runs` store start, end, status, error summary, and cursor result; failed runs stay queryable; retry evidence is preserved | migration, service test, log sample |
| `US-05-01-04` | 5 | Draft | Must | Sprint 1 | `source_items` map upstream ids to internal ids; upsert path is idempotent; duplicate import test passes | migration, replay test, idempotency test |
| `US-05-01-05` | 5 | Draft | Must | Sprint 2 | integrations shell shows available providers; connected, syncing, empty, and error states exist; copy is consistent with current product behavior | UI demo or screenshot, smoke test |
| `US-05-01-06` | 3 | Draft | Must | Sprint 2 | connector contract covers `connect`, `validate`, `backfill`, `sync_incremental`, `normalize`, and `disconnect`; first live connector uses it; contract is documented | shared interface PR, unit test, doc link |

### `EPIC-05-02` Zendesk Support Connector MVP

PRD:

- `PRD-05-02`

Epic goal:

- prove the connector architecture with one live Zendesk support source

Dependencies:

- `EPIC-05-01`
- `D-05-01` provider choice

Exit criteria:

- support data can be connected, backfilled, normalized, and refreshed

| Story ID | Pts | State | Class | Target Sprint | Ready Requirements | Acceptance Evidence |
|---|---:|---|---|---|---|---|
| `US-05-02-01` | 5 | Draft | Must | Sprint 2 | Zendesk credential validation succeeds and fails clearly; connection record stores only secret reference, not raw secret; integrations shell shows connected or error state | integration demo, API test, security review note |
| `US-05-02-02` | 5 | Draft | Must | Sprint 3 | first historical backfill imports bounded history; duplicate import test passes; sync run records counts and status | replay test, sync log sample, DB verification |
| `US-05-02-03` | 5 | Draft | Must | Sprint 3 | incremental sync uses a cursor; retry is idempotent; last successful sync is visible | worker test, cursor test, UI or API proof |
| `US-05-02-04` | 5 | Draft | Must | Sprint 4 | Zendesk tickets normalize into shared signal shape; source linkback is preserved; feed and theme queries can consume normalized rows | normalization tests, API integration test |
| `US-05-02-05` | 3 | Draft | Must | Sprint 4 | last sync, error state, and manual re-sync are visible; operator can tell what failed; re-sync does not bypass auditability | UI demo, smoke test, log sample |

### `EPIC-05-03` Survey And NPS Import MVP

PRD:

- `PRD-05-03`

Epic goal:

- give users a low-friction path to bring survey evidence into the product

Dependencies:

- `D-05-02` `signals` schema approval

Exit criteria:

- users can upload valid CSVs, preview mapped data, and import survey signals

| Story ID | Pts | State | Class | Target Sprint | Ready Requirements | Acceptance Evidence |
|---|---:|---|---|---|---|---|
| `US-05-03-01` | 3 | Draft | Must | Sprint 2 | CSV template exists; required columns and validation messages are defined; invalid files fail clearly | template file, validation test, UI proof |
| `US-05-03-02` | 3 | Draft | Must | Sprint 3 | preview step shows rows and mapped fields; warnings are visible before import; user can cancel safely | UI demo, smoke test |
| `US-05-03-03` | 5 | Draft | Must | Sprint 4 | survey rows normalize into shared signals; survey metadata is retained; feed, themes, and score inputs can query them | normalization tests, API test |
| `US-05-03-04` | 2 | Draft | Must | Sprint 3 | import history shows status, counts, and failure summary; failed imports are visible after page refresh; messages are plain language | UI proof, integration test |

### `EPIC-05-04` Unified Feed And Source-Aware Themes

PRD:

- `PRD-05-04`

Epic goal:

- surface multi-source evidence clearly enough that users feel the product shift immediately

Dependencies:

- `EPIC-05-02`
- `EPIC-05-03`

Exit criteria:

- mixed-source feed works
- theme details show source breakdowns and evidence links

| Story ID | Pts | State | Class | Target Sprint | Ready Requirements | Acceptance Evidence |
|---|---:|---|---|---|---|---|
| `US-05-04-01` | 5 | Draft | Must | Sprint 4 | unified feed renders support, survey, and interview rows consistently; source badges and timestamps are clear; mixed-source ordering is predictable | API integration test, UI demo |
| `US-05-04-02` | 3 | Draft | Must | Sprint 5 | filters by source, date, and sentiment exist; filters combine correctly; chosen filters persist during normal analysis flow | UI demo, smoke test |
| `US-05-04-03` | 5 | Draft | Must | Sprint 5 | theme detail shows counts by source type; evidence sections are source-aware; copy uses "supporting evidence" not causation language | UI proof, acceptance review note |
| `US-05-04-04` | 3 | Draft | Must | Sprint 5 | every feed and theme item links back to original evidence; linkbacks are visible in detail views; missing link state is handled clearly | UI demo, integration test |
| `US-05-04-05` | 3 | Draft | Should | Sprint 6 stretch | named filter sets can be created, saved, and reapplied; saved views do not add workflow complexity; no regression to core feed usability | UI demo, smoke test |

### `EPIC-05-05` Impact Score v1 And Priority Board

PRD:

- `PRD-05-05`

Epic goal:

- turn multi-source evidence into an explainable prioritization surface

Dependencies:

- `EPIC-05-04`

Exit criteria:

- score v1 exists and is inspectable
- priority board is usable in beta

| Story ID | Pts | State | Class | Target Sprint | Ready Requirements | Acceptance Evidence |
|---|---:|---|---|---|---|---|
| `US-05-05-01` | 5 | Draft | Must | Sprint 5 | score formula uses frequency, recency, negative sentiment, and source diversity only; rank order is deterministic; formula is documented | score tests, formula note, sample output |
| `US-05-05-02` | 3 | Draft | Must | Sprint 6 | score breakdown is visible in theme detail and board expansion; users can see why rank changed; wording avoids fake precision | UI proof, acceptance review note |
| `US-05-05-03` | 5 | Draft | Must | Sprint 6 | board renders ranked themes; cards show source breakdown and evidence preview; board is usable without turning into a task manager | UI demo, smoke test |
| `US-05-05-04` | 3 | Draft | Must | Sprint 6 | users can pin or monitor themes; override state is lightweight; behavior is clearly not a workflow tracker | UI demo, integration test |
| `US-05-05-05` | 5 | Draft | Could | Sprint 7 stretch | collections and theme merge solve a real pilot pain point; merge preserves evidence history; no delay to `Must` quality work | UI demo, regression test |

### `EPIC-05-06` Quality, Security, And Beta Rollout

PRD:

- `PRD-05-01`
- `PRD-05-02`
- `PRD-05-03`
- `PRD-05-04`
- `PRD-05-05`

Epic goal:

- make the release safe enough to put in front of pilot users

Dependencies:

- all prior `v0.5` epics
- `D-05-04` smoke test decision
- `D-05-05` observability decision
- `D-05-06` trust package decision

Exit criteria:

- QA pass complete
- trust copy updated
- release checklist passed

| Story ID | Pts | State | Class | Target Sprint | Ready Requirements | Acceptance Evidence |
|---|---:|---|---|---|---|---|
| `US-05-06-01` | 5 | Draft | Must | Sprint 7 | smoke coverage exists for integrations shell, survey import, feed, and board; runs in CI; failing smoke blocks ship gate | CI run, smoke test file list |
| `US-05-06-02` | 5 | Draft | Must | Sprint 7 | connector contract and replay tests cover happy path, retry path, and idempotency; provider changes are detectable; fixtures are reusable | replay suite, contract tests, run output |
| `US-05-06-03` | 3 | Draft | Must | Sprint 6 | in-product permissions and storage copy match actual behavior; disconnect and delete behavior are explained; support docs and UI copy are aligned | published copy, review note |
| `US-05-06-04` | 3 | Draft | Must | Sprint 6 | sync success, duplicate rate, and time-to-first-insight metrics are visible in one review surface; founder can review them during pilot; alert thresholds are defined | dashboard screenshot, metric names, alert note |
| `US-05-06-05` | 2 | Draft | Must | Sprint 7 | pilot list exists; rollout notes and rollback plan are written; go/no-go checklist is linked and current | rollout doc, checklist, sign-off note |

---

## 10. `v0.51` Forecast Backlog

Important note:

- all `v0.51` stories remain `Outline`
- they do not need full acceptance evidence yet
- do not move them to `Draft` until `G1M` passes

### `EPIC-051-01` Fireflies Connection And Credential Flow

PRD:

- `PRD-051-01`

Epic goal:

- let a user connect Fireflies once, clearly and safely

| Story ID | Pts | State | Earliest Sprint | Forecast Note |
|---|---:|---|---|---|
| `US-051-01-01` | 2 | Outline | Sprint 8 | provider card and entry point |
| `US-051-01-02` | 5 | Outline | Sprint 8 | credential validation with clear failure states |
| `US-051-01-03` | 5 | Outline | Sprint 8 | secrets stored by reference only |
| `US-051-01-04` | 3 | Outline | Sprint 8 | trust guidance during setup |

### `EPIC-051-02` Fireflies Backfill And Interview Materialization

PRD:

- `PRD-051-01`

Epic goal:

- import historical Fireflies meetings and make them behave like native Spec10x interviews

| Story ID | Pts | State | Earliest Sprint | Forecast Note |
|---|---:|---|---|---|
| `US-051-02-01` | 5 | Outline | Sprint 9 | historical backfill |
| `US-051-02-02` | 5 | Outline | Sprint 9 | interview materialization |
| `US-051-02-03` | 3 | Outline | Sprint 9 | metadata preservation |
| `US-051-02-04` | 5 | Outline | Sprint 9 | duplicate prevention |

### `EPIC-051-03` Incremental Sync, Disconnect, And Admin Controls

PRD:

- `PRD-051-01`

Epic goal:

- keep the interview library fresh after first connection and make the connector manageable

| Story ID | Pts | State | Earliest Sprint | Forecast Note |
|---|---:|---|---|---|
| `US-051-03-01` | 5 | Outline | Sprint 10 | scheduled incremental sync |
| `US-051-03-02` | 3 | Outline | Sprint 10 | status and error visibility |
| `US-051-03-03` | 3 | Outline | Sprint 10 | manual re-sync |
| `US-051-03-04` | 3 | Outline | Sprint 10 | disconnect behavior |
| `US-051-03-05` | 5 | Outline | Sprint 11 | imported-data delete behavior |

### `EPIC-051-04` Hardening And Pilot Rollout

PRD:

- `PRD-051-01`

Epic goal:

- make auto-sync trustworthy enough for real customer use

| Story ID | Pts | State | Earliest Sprint | Forecast Note |
|---|---:|---|---|---|
| `US-051-04-01` | 5 | Outline | Sprint 11 | failure-path tests |
| `US-051-04-02` | 3 | Outline | Sprint 11 | audit logs and sync metrics |
| `US-051-04-03` | 3 | Outline | Sprint 10 | trust docs and help content |
| `US-051-04-04` | 2 | Outline | Sprint 11 | pilot rollout and feedback plan |

---

## 11. `v0.52` Forecast Backlog

Important note:

- all `v0.52` stories remain `Outline`
- do not move them to `Draft` until `G2M` passes

### `EPIC-052-01` Analytics Source Extension

PRD:

- `PRD-052-01`

Epic goal:

- extend the common signal model so analytics data can enter without breaking clarity

| Story ID | Pts | State | Earliest Sprint | Forecast Note |
|---|---:|---|---|---|
| `US-052-01-01` | 5 | Outline | Sprint 12 | analytics entities and normalized fields |
| `US-052-01-02` | 3 | Outline | Sprint 12 | clear source labels and metric semantics |
| `US-052-01-03` | 3 | Outline | Sprint 12 | false-correlation guardrails |

### `EPIC-052-02` First Analytics Connector

PRD:

- `PRD-052-01`

Epic goal:

- bring one analytics provider into the shared evidence system

| Story ID | Pts | State | Earliest Sprint | Forecast Note |
|---|---:|---|---|---|
| `US-052-02-01` | 5 | Outline | Sprint 13 | provider connect and validate |
| `US-052-02-02` | 5 | Outline | Sprint 13 | historical backfill |
| `US-052-02-03` | 5 | Outline | Sprint 13 | normalization into common signals |
| `US-052-02-04` | 3 | Outline | Sprint 13 | sync health visibility |

### `EPIC-052-03` Score v2, Trends, And Evidence UX

PRD:

- `PRD-052-02`

Epic goal:

- improve prioritization quality without making the system feel magical or dishonest

| Story ID | Pts | State | Earliest Sprint | Forecast Note |
|---|---:|---|---|---|
| `US-052-03-01` | 5 | Outline | Sprint 14 | trend direction |
| `US-052-03-02` | 5 | Outline | Sprint 14 | score change explanations |
| `US-052-03-03` | 3 | Outline | Sprint 14 | source filters in evidence panels |
| `US-052-03-04` | 2 | Outline | Sprint 14 | clearer caution language |

### `EPIC-052-04` Hardening And Release Rollout

PRD:

- `PRD-052-01`
- `PRD-052-02`

Epic goal:

- ship analytics safely rather than noisily

| Story ID | Pts | State | Earliest Sprint | Forecast Note |
|---|---:|---|---|---|
| `US-052-04-01` | 3 | Outline | Sprint 15 | performance checks |
| `US-052-04-02` | 5 | Outline | Sprint 15 | replay and sync tests |
| `US-052-04-03` | 2 | Outline | Sprint 15 | analytics release checklist |

---

## 12. Optional `v0.53` Forecast Backlog

Important note:

- all `v0.53` stories remain `Outline`
- do not open this release until `G3M` says it is worth doing

### `EPIC-053-01` Second Interview Connector

PRD:

- `PRD-053-01`

Epic goal:

- prove the interview connector architecture is reusable across more than one provider

| Story ID | Pts | State | Earliest Sprint | Forecast Note |
|---|---:|---|---|---|
| `US-053-01-01` | 5 | Outline | Sprint 16 | second provider connection flow |
| `US-053-01-02` | 5 | Outline | Sprint 16 | historical backfill for second provider |
| `US-053-01-03` | 5 | Outline | Sprint 17 | incremental sync for second provider |
| `US-053-01-04` | 5 | Outline | Sprint 17 | imported interviews behave like native records |

### `EPIC-053-02` Connector Platform Hardening

PRD:

- `PRD-053-01`

Epic goal:

- reduce the operational cost of having multiple live connectors

| Story ID | Pts | State | Earliest Sprint | Forecast Note |
|---|---:|---|---|---|
| `US-053-02-01` | 5 | Outline | Sprint 17 | stronger retry and dead-letter handling |
| `US-053-02-02` | 5 | Outline | Sprint 17 | key rotation and credential refresh |
| `US-053-02-03` | 3 | Outline | Sprint 18 | runbooks and dashboard improvements |

### `EPIC-053-03` Optional Ownership And Collaboration Polish

PRD:

- `PRD-053-01`

Epic goal:

- improve organization readiness only if pilot demand proves it is worth doing

| Story ID | Pts | State | Earliest Sprint | Forecast Note |
|---|---:|---|---|---|
| `US-053-03-01` | 5 | Outline | Sprint 18 | ownership model revisit |
| `US-053-03-02` | 3 | Outline | Sprint 18 | lightweight ownership or comment fields |

---

## 13. Sprint Plan And Capacity

### 13.1 `v0.5` sprint plan

| Sprint | Window | Committed Points | Committed Work | Stretch Work | Sprint Exit |
|---|---|---:|---|---|---|
| Sprint 0 | Mar 16 - Mar 22, 2026 | 0 | decisions, PRDs, risk register, release metrics draft, first two sprint loads | none | pass `G0` |
| Sprint 1 | Mar 23 - Mar 29, 2026 | 16 | `US-05-01-01`, `US-05-01-02`, `US-05-01-03`, `US-05-01-04` | none | schema and sync model exist in code and migration form |
| Sprint 2 | Mar 30 - Apr 05, 2026 | 16 | `US-05-01-05`, `US-05-01-06`, `US-05-02-01`, `US-05-03-01` | none | integrations shell is visible and first connector path can validate |
| Sprint 3 | Apr 06 - Apr 12, 2026 | 15 | `US-05-02-02`, `US-05-02-03`, `US-05-03-02`, `US-05-03-04` | none | support backfill and incremental basics work; survey import preview is safe |
| Sprint 4 | Apr 13 - Apr 19, 2026 | 18 | `US-05-02-04`, `US-05-02-05`, `US-05-03-03`, `US-05-04-01` | none | support and survey both reach shared signals; unified feed is usable |
| Sprint 5 | Apr 20 - Apr 26, 2026 | 16 | `US-05-04-02`, `US-05-04-03`, `US-05-04-04`, `US-05-05-01` | none | feed filtering, source-aware themes, linkbacks, and score engine v1 exist |
| Sprint 6 | Apr 27 - May 03, 2026 | 17 | `US-05-05-02`, `US-05-05-03`, `US-05-05-04`, `US-05-06-03`, `US-05-06-04` | `US-05-04-05` if all `Must` stories are green by Thursday | board is usable, trust copy is aligned, observability is live |
| Sprint 7 | May 04 - May 10, 2026 | 12 | `US-05-06-01`, `US-05-06-02`, `US-05-06-05` | `US-05-05-05` if release remains green and no Sev-1 or Sev-2 surprise emerges | ship decision at `G1` |
| `v0.5` measure window | May 11 - May 24, 2026 | n/a | pilot rollout, metric review, customer feedback synthesis | none | pass `G1M` before opening `v0.51` |

Non-story enablers that must land no later than the sprint shown:

- by Sprint 0 exit: smoke test runner chosen, observability metrics defined, trust package outline agreed
- by Sprint 2 exit: CI job exists for smoke coverage, even if the final suite is not complete
- by Sprint 4 exit: sync metric review surface exists in a reviewable form

### 13.2 `v0.51` and `v0.52` earliest sprint loads

These are forecast loads only. They are intentionally not commitment-ready yet.

| Sprint | Window | Forecast Points | Planned Work |
|---|---|---:|---|
| Sprint 8 | May 25 - May 31, 2026 | 15 | `US-051-01-01`, `US-051-01-02`, `US-051-01-03`, `US-051-01-04` |
| Sprint 9 | Jun 01 - Jun 07, 2026 | 18 | `US-051-02-01`, `US-051-02-02`, `US-051-02-03`, `US-051-02-04` |
| Sprint 10 | Jun 08 - Jun 14, 2026 | 17 | `US-051-03-01`, `US-051-03-02`, `US-051-03-03`, `US-051-03-04`, `US-051-04-03` |
| Sprint 11 | Jun 15 - Jun 21, 2026 | 15 | `US-051-03-05`, `US-051-04-01`, `US-051-04-02`, `US-051-04-04` |
| `v0.51` measure window | Jun 22 - Jul 05, 2026 | n/a | rollout, telemetry review, trust-friction review |
| Sprint 12 | Jul 06 - Jul 12, 2026 | 11 | `US-052-01-01`, `US-052-01-02`, `US-052-01-03` |
| Sprint 13 | Jul 13 - Jul 19, 2026 | 18 | `US-052-02-01`, `US-052-02-02`, `US-052-02-03`, `US-052-02-04` |
| Sprint 14 | Jul 20 - Jul 26, 2026 | 15 | `US-052-03-01`, `US-052-03-02`, `US-052-03-03`, `US-052-03-04` |
| Sprint 15 | Jul 27 - Aug 02, 2026 | 10 | `US-052-04-01`, `US-052-04-02`, `US-052-04-03` |
| `v0.52` measure window | Aug 03 - Aug 16, 2026 | n/a | rollout, clarity review, false-correlation review |

### 13.3 Optional `v0.53` earliest sprint loads

| Sprint | Window | Forecast Points | Planned Work |
|---|---|---:|---|
| Sprint 16 | Aug 17 - Aug 23, 2026 | 10 | `US-053-01-01`, `US-053-01-02` |
| Sprint 17 | Aug 24 - Aug 30, 2026 | 15 | `US-053-01-03`, `US-053-01-04`, `US-053-02-01` |
| Sprint 18 | Aug 31 - Sep 06, 2026 | 16 | `US-053-02-02`, `US-053-02-03`, `US-053-03-01`, `US-053-03-02` |

---

## 14. Pull Request Strategy

Large releases fail when PRs become too large to review or reason about.

### 14.1 PR slicing rules

- one PR should ideally map to one story or one vertical slice of a story
- avoid mega PRs that combine migration, backend logic, UI, copy, and tests for unrelated work
- every PR should include its story ID in the title or description
- every PR should state what acceptance criteria it covers
- every PR for operational work should state what logs or metrics were added

Recommended PR size:

- small to medium
- reviewable in under 30 minutes

### 14.2 Evidence block template for PRs

Every PR description should include:

```text
Story: US-XX-YY-ZZ
PRD: PRD-XX-YY
Acceptance criteria covered:
- ...
- ...

Evidence:
- Tests:
- UI proof:
- Metrics/logs:
- Docs/copy:
```

---

## 15. Sprint Review Checklist

Every sprint review should answer:

- what stories were accepted?
- what slipped, and why?
- what new information changed priority or scope?
- what technical debt was created?
- what moved above or below the release cut line?
- what gate risk changed from green to yellow or red?

For connector-related sprints, also answer:

- did we request more permission than intended?
- can a failed sync be debugged from logs alone?
- can the user understand connection state without support?
- does disconnect and imported-data delete behavior still match the docs?

---

## 16. Release Go Or No-Go Checklist

Before shipping any release in the `v0.5x` family, confirm:

- release exit criteria are met
- all `Must` stories are accepted
- open defects meet the severity threshold
- key happy-path flows were tested
- key failure-path flows were tested
- docs and trust copy match actual behavior
- metrics and logs exist for new operational surfaces
- rollback or containment plan is understood
- release notes and pilot communication are ready

For connector releases, also confirm:

- disconnect behavior works
- imported-data delete behavior works
- secrets are not stored in ordinary DB fields
- provider-specific error states are understandable
- audit trail or sync trail is reviewable

Allowed ship decisions:

- `Go`
- `Hold`
- `Go with documented Sev-2 exceptions only`

---

## 17. Post-Release Measurement Plan

Each release should be measured for two weeks after rollout.

### `v0.5` measurement plan

| Metric | Why It Matters | Source Of Truth | Decision Use |
|---|---|---|---|
| time from source connection or import to first useful insight | tells whether the product becomes useful quickly enough | sync metrics and pilot notes | if slow, improve sync or import experience before opening more scope |
| percent of themes with multi-source evidence | tells whether the core wedge is actually visible | analytics query or dashboard | if low, improve normalization and evidence presentation before scaling connectors |
| weekly usage of unified feed | tells whether users actually use the new evidence surface | product analytics | if weak, fix feed clarity before expanding board or score work |
| weekly usage of priority board | tells whether the board helps PMs decide what to inspect | product analytics | if weak, refine board or score explanation before `v0.51` polish |
| user-rated trust in score v1 | tells whether the score helps or harms credibility | pilot survey and interviews | if weak, hold advanced scoring work and improve explainability |

### `v0.51` measurement plan

| Metric | Why It Matters | Source Of Truth | Decision Use |
|---|---|---|---|
| Fireflies connection success rate | tells whether setup is practically usable | connector metrics | if weak, improve onboarding before provider number two |
| time from connection to completed backfill | tells whether first-use value is too slow | sync metrics | if slow, reduce backfill scope or improve batching before broad rollout |
| sync success rate | tells whether auto-sync is trustworthy | sync metrics | if weak, do not open second interview connector |
| percent of supported users preferring auto-sync over manual upload | tells whether the release solved the real workflow pain | pilot feedback | if weak, improve trust and setup UX before more connector work |
| support tickets related to trust, permissions, or confusion | tells whether fear or confusion blocks adoption | support log | if high, fix trust materials before growth |

### `v0.52` measurement plan

| Metric | Why It Matters | Source Of Truth | Decision Use |
|---|---|---|---|
| analytics sync success rate | tells whether analytics ingestion is operationally stable | sync metrics | if weak, harden before adding more analytics sources |
| number of themes with both qualitative and quantitative evidence | tells whether the mixed evidence promise is real | analytics query | if low, fix normalization or evidence UX |
| user comprehension of trend direction and score changes | tells whether the product stays interpretable | pilot interviews and survey | if weak, simplify score and trend UX before expanding |
| feedback caused by confusing or misleading correlations | tells whether trust is being damaged | support log and pilot review | if high, strengthen guardrails before any broader rollout |

---

## 18. Final Delivery Discipline

The tracker should be run with one core discipline:

- do not start the next release until the current release has both shipped and been measured

That matters because the whole `v0.5x` family is connected:

- `v0.5` proves Spec10x can combine evidence
- `v0.51` proves Spec10x can stay current automatically
- `v0.52` proves Spec10x can connect customer language to product behavior without losing trust
- `v0.53` is optional validation that the connector platform is reusable and supportable

If the team protects cut lines, keeps only the current release at `Ready` detail, and insists on gate evidence before moving forward, this tracker is realistic for a solo founder plus AI agents.
