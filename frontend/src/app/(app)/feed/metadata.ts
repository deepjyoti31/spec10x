import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Feed | Spec10x',
    description: 'Review unified evidence signals across interviews, support tickets, and surveys.',
};
